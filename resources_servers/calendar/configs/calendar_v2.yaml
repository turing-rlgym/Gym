calendar:
  resources_servers:
    calendar:
      entrypoint: app.py
      domain: agent
      verified: false
      description: Multi-turn calendar scheduling dataset. User states events and constraints in natural language; model schedules events to satisfy all constraints.
      value: Improve multi-turn instruction following capabilities
calendar_simple_agent:
  responses_api_agents:
    simple_agent:
      entrypoint: app.py
      resources_server:
        type: resources_servers
        name: calendar
      model_server:
        type: responses_api_models
        name: policy_model
      datasets:
      - name: train
        type: train
        jsonl_fpath: resources_servers/calendar/data/train_v2.jsonl
        huggingface_identifier:
          repo_id: nvidia/Nemotron-RL-Instruction-Following-Calendar-v2
          artifact_fpath: train.jsonl
        license: TBD
      - name: validation
        type: validation
        jsonl_fpath: resources_servers/calendar/data/validation_v2.jsonl
        huggingface_identifier:
          repo_id: nvidia/Nemotron-RL-Instruction-Following-Calendar-v2
          artifact_fpath: validation.jsonl
        license: TBD
